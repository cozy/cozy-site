// Containers tags
//
// Apply styles to html, body and main containers classes

body
  padding-top .938rem
  font-family: $ff-common
  font-size: $fs-base
  line-height: $lh
  color: $txt-clr-base
  background-color: $body-bkg